export class Bottle {
  constructor(public brand: string, public pricePerBottle: number, public pricePerShot: number, public proof: number, public distillery: string, public shots: number, public flavor: string, public color: string, public alcoholType: string) { }
}
